{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:4200", // Adjust to your application's URL
            "webRoot": "${workspaceFolder}" // Adjust if your web root is different
        }
    ]
}